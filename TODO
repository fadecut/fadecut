1st release
* Check for necessary binaries (marco)
* Renaming files without white-space (tinu)
* ogg default encoding? (marco)
* auto-select ogg encoding if mp3 lame is not available (marco)
* version numbering, may be usful information for debian packaging (tinu)

later
* debian packet
* think about where to save which configuration options (maybe ~/.fadecut/* ?)
* history/statistics
* cross correlation with well known ad files
* maybe we could use ffmpeg instead of sox/lame 
