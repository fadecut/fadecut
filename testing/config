
# -----------------------------------------------------------------------------
MYSELF="$(readlink -f $0)"                      # will use it to fork myself
TESTFOLDER="$(readlink -f $(dirname $0))"       # where testscript is located
PWD=$(pwd)                                      # working folder

LOCKFILE=/tmp/fadecuttesting.lk
HOME_FADECUT=${HOME}/.fadecut
BIN_FADECUT="$TESTFOLDER"/../fadecut
RECIPES="$TESTFOLDER/recipes"

